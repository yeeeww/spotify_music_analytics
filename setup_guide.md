# Spotify Music Analytics 설치 및 실행 가이드

## 📋 목차
1. [시스템 요구사항](#시스템-요구사항)
2. [설치 방법](#설치-방법)
3. [데이터 준비](#데이터-준비)
4. [실행 방법](#실행-방법)
5. [문제 해결](#문제-해결)

---

## 시스템 요구사항

- **Python**: 3.10 이상
- **운영체제**: Windows, macOS, Linux
- **메모리**: 최소 4GB RAM
- **저장공간**: 최소 500MB

---

## 설치 방법

### 1. Python 설치 확인

```bash
python --version
```

Python 3.10 이상이 설치되어 있어야 합니다.

### 2. 프로젝트 클론 또는 다운로드

```bash
git clone <repository-url>
cd spotify_music_analytics
```

### 3. 가상환경 생성 (권장)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Mac/Linux:**
```bash
python -m venv venv
source venv/bin/activate
```

### 4. 패키지 설치

```bash
pip install -r requirements.txt
```

### 5. 환경 변수 설정

1. `.env.example` 파일을 `.env`로 복사:

**Windows:**
```bash
copy .env.example .env
```

**Mac/Linux:**
```bash
cp .env.example .env
```

2. `.env` 파일을 열어 Gemini API 키 입력:

```
GEMINI_API_KEY=your_actual_api_key_here
```

**Gemini API 키 발급 방법:**
- https://makersuite.google.com/app/apikey 방문
- Google 계정으로 로그인
- "Create API Key" 클릭
- 생성된 키를 복사하여 `.env` 파일에 붙여넣기

---

## 데이터 준비

### 1. 데이터셋 다운로드

**방법 1: Kaggle 웹사이트에서 다운로드**

1. https://www.kaggle.com/datasets/maharshipandya/-spotify-tracks-dataset 방문
2. "Download" 버튼 클릭
3. 다운로드한 `dataset.csv` 파일을 `data/raw/` 폴더에 저장

**방법 2: Kaggle API 사용 (선택사항)**

```bash
pip install kaggle
kaggle datasets download -d maharshipandya/-spotify-tracks-dataset
unzip spotify-tracks-dataset.zip -d data/raw/
```

### 2. 데이터 검증

```bash
python scripts/download_data.py
```

이 스크립트는 데이터 파일이 올바르게 위치했는지 확인합니다.

### 3. 데이터 전처리

```bash
python scripts/preprocess_data.py
```

이 과정은 다음을 수행합니다:
- 중복 제거
- 결측치 처리
- 데이터 타입 최적화
- 이상치 제거

예상 소요 시간: 1-3분

### 4. 데이터베이스 구축

```bash
python scripts/build_database.py
```

이 과정은 다음을 수행합니다:
- SQLite 데이터베이스 생성
- 테이블 생성 (tracks, genres, artists, albums)
- 인덱스 생성
- 통계 뷰 생성

예상 소요 시간: 2-5분

---

## 실행 방법

### 1. Streamlit 앱 실행

```bash
streamlit run app.py
```

### 2. 브라우저에서 접속

자동으로 브라우저가 열리며, 다음 주소로 접속됩니다:
```
http://localhost:8501
```

### 3. 앱 사용

1. **메인 페이지**: 프로젝트 개요 및 데이터베이스 상태 확인
2. **데이터 탐색**: 테이블 구조 및 데이터 미리보기
3. **자연어 질의**: 한국어 질문으로 데이터 조회
4. **분석 리포트**: 종합적인 데이터 분석 및 시각화

---

## 문제 해결

### 문제 1: ModuleNotFoundError

**증상:**
```
ModuleNotFoundError: No module named 'streamlit'
```

**해결:**
```bash
pip install -r requirements.txt
```

### 문제 2: Gemini API 키 오류

**증상:**
```
ValueError: Gemini API 키가 설정되지 않았습니다.
```

**해결:**
1. `.env` 파일이 프로젝트 루트에 있는지 확인
2. `.env` 파일에 `GEMINI_API_KEY=your_key` 형식으로 키가 입력되었는지 확인
3. API 키가 유효한지 확인

### 문제 3: 데이터베이스 파일을 찾을 수 없음

**증상:**
```
❌ 데이터베이스 파일을 찾을 수 없습니다.
```

**해결:**
1. `data/raw/dataset.csv` 파일이 있는지 확인
2. 데이터 전처리 실행: `python scripts/preprocess_data.py`
3. 데이터베이스 구축 실행: `python scripts/build_database.py`

### 문제 4: 포트가 이미 사용 중

**증상:**
```
OSError: [Errno 48] Address already in use
```

**해결:**
다른 포트로 실행:
```bash
streamlit run app.py --server.port 8502
```

### 문제 5: 메모리 부족

**증상:**
앱이 느리거나 멈춤

**해결:**
1. 다른 프로그램 종료
2. 데이터 샘플링 크기 줄이기 (코드에서 LIMIT 값 조정)

---

## 추가 정보

### 프로젝트 구조

```
spotify_music_analytics/
├── app.py                      # 메인 앱
├── requirements.txt            # 패키지 의존성
├── .env                        # 환경 변수 (직접 생성)
├── README.md                   # 프로젝트 문서
├── setup_guide.md             # 이 파일
├── data/
│   ├── raw/                    # 원본 데이터
│   │   └── dataset.csv         # Kaggle 데이터셋
│   ├── processed/              # 전처리된 데이터
│   └── spotify.db              # SQLite 데이터베이스
├── scripts/
│   ├── download_data.py        # 데이터 검증
│   ├── preprocess_data.py      # 데이터 전처리
│   └── build_database.py       # DB 구축
├── modules/
│   ├── database.py             # DB 연결 및 쿼리
│   ├── llm.py                  # Gemini API 연동
│   └── visualization.py        # 시각화 함수
└── pages/
    ├── 1_📊_데이터_탐색.py
    ├── 2_💬_자연어_질의.py
    └── 3_📈_분석_리포트.py
```

### 유용한 명령어

**가상환경 비활성화:**
```bash
deactivate
```

**패키지 업데이트:**
```bash
pip install --upgrade -r requirements.txt
```

**캐시 삭제:**
```bash
streamlit cache clear
```

---

## 지원

문제가 계속되면 다음을 확인하세요:
1. Python 버전이 3.10 이상인지
2. 모든 패키지가 올바르게 설치되었는지
3. 데이터 파일이 올바른 위치에 있는지
4. API 키가 유효한지

---

**Happy Analyzing! 🎵**

